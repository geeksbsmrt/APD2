<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adamcrawford.geoscavenge" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GuessActivity"
            android:label="@string/title_activity_guess" >
        </activity>
        <activity
            android:name=".hunt.NewHuntActivity"
            android:label="@string/title_activity_new_hunt"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.adamcrawford.geoscavenge.MainActivity" />
        </activity>
        <activity
            android:name=".hunt.NewEndpointActivity"
            android:label="@string/title_activity_new_endpoint"
            android:parentActivityName=".hunt.NewHuntActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.adamcrawford.geoscavenge.hunt.NewHuntActivity" />
        </activity>
        <service android:name=".data.SyncService"/>
    </application>

</manifest>
